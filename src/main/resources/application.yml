spring:
  application:
    name: trimly

  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:trimly}
    username: ${DB_USER:trimly}
    password: ${DB_PASSWORD:trimly}

  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false

  flyway:
    enabled: true

server:
  port: ${SERVER_PORT:8080}

logging:
  level:
    io.github.david7777k.trimly: DEBUG
